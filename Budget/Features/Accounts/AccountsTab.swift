import SwiftUI

/// Comptes tab. Phase 0 ships a shell; the full account experience lands
/// in Phase 2.
struct AccountsTab: View {
    var body: some View {
        ComingSoonView(
            title: "Comptes",
            message: "La gestion des comptes arrive dans une prochaine étape."
        )
    }
}
