{
  "name": "budget-build",
  "private": true,
  "description": "Outillage de build W9 — versions épinglées exactes (même motif que playwright en CI).",
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
