/* W9.1 — Point d'entrée TypeScript du chantier « PWA modulaire ».
   RIEN n'est encore servi depuis ici : le monofichier webapp/index.html
   reste LA source vivante jusqu'à W9.8 (Work Order W9). Ce fichier
   existe pour que la porte de types du build ait une vraie source à
   vérifier ; W9.2 y amènera les premiers calculs purs extraits
   (snapshot, fortune, taux datés) avec le comparateur canon en gate. */

/** Version du pipeline de build — bougera à chaque étape du chantier W9. */
export const BUILD_PIPELINE_VERSION = "w9.1";
